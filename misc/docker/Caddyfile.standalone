:80 {
    handle_path /api/* {
        reverse_proxy localhost:8080
    }

    handle /health {
        respond "ok" 200
    }

    handle {
        root * /app/front
        encode gzip
        try_files {path} /index.html
        file_server
    }
}